# Replit Migration Plan

- [ ] Understand Replit configuration and project stack (read `package.json`, `.replit`, etc.)
- [ ] Install dependencies (`npm install`)
- [ ] Set up local environment variables (e.g. database connections) replacing Replit-specific ones
- [ ] Start the development server and verify everything works locally
